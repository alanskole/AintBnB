<Page
    x:Class="AintBnB.Views.CreateUserPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" >
        <Grid>
            <TextBox Text="{x:Bind ViewModel.User.UserName, Mode=TwoWay}" PlaceholderText="Username" Name="userName" HorizontalAlignment="Left" Margin="260,100,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="58" Width="212"/>
            <PasswordBox Password="{x:Bind ViewModel.User.Password, Mode=TwoWay}" PlaceholderText="Password" Name="password" HorizontalAlignment="Left" Margin="260,200,0,0" VerticalAlignment="Top" Height="58" Width="212"/>
            <TextBox Text="{x:Bind ViewModel.User.FirstName, Mode=TwoWay}" PlaceholderText="First name" Name="firstName" HorizontalAlignment="Left" Margin="260,300,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="58" Width="212"/>
            <TextBox Text="{x:Bind ViewModel.User.LastName, Mode=TwoWay}" PlaceholderText="Last name" Name="lastName" HorizontalAlignment="Left" Margin="260,400,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="58" Width="212"/>
            <Button Click="Button_Click" Content="Create User" Margin="313,500,0,0" VerticalAlignment="Top" Width="106"/>
        </Grid>
    </ScrollViewer>
</Page>
